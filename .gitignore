.env
node_modules/
contracts/out/
contracts/cache/
contracts/broadcast/
cli/.state/
*.log
docs/10-PENJELASAN-ID.md

# Next.js (web/)
.next/
web/out/
web/next-env.d.ts
# Impeccable runtime state (pids, ports); the surface brief itself is committed
web/.impeccable/questions/
web/tsconfig.tsbuildinfo

# One package manager. npm is the tested path; a stray pnpm lockfile makes Vercel
# pick a dependency tree nobody built against.
pnpm-lock.yaml
yarn.lock
